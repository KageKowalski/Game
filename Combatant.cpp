#include "Combatant.h"

Combatant::Combatant(int vit, int str, int def, int lck, int spd, int tme, int gld, int exp) : _vit(vit), _str(str), _def(def), _lck(lck), _spd(spd), _tme(tme), _gld(gld), _exp(exp), Entity("Generic Combatant")
{}
